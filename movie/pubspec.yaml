name: movie
description: Movie module for ditonton.

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../core
  dartz: ^0.10.1
  equatable: ^2.0.8
  cached_network_image: ^3.4.1
  flutter_rating_bar: ^4.0.1
  flutter_bloc: ^9.1.1
  get_it: ^9.2.1

dev_dependencies:
  mockito: ^5.6.4
  flutter_test:
    sdk: flutter
  bloc_test: ^10.0.0
  build_runner: ^2.13.1
